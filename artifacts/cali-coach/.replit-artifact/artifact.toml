kind = "web"
previewPath = "/"
title = "CaliCoach"
version = "1.0.0"
id = "artifacts/cali-coach"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25344

[services.development]
run = "pnpm --filter @workspace/cali-coach run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/cali-coach", "run", "build" ]
publicDir = "artifacts/cali-coach/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25344"
BASE_PATH = "/"
